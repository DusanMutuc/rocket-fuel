// src/types/TaskType.ts
export interface TaskType {
    task_type_id: number;      // Unique identifier for the task type
    name: string;              // Name of the task type (e.g., "Asks", "Follow-ups", etc.)
}
